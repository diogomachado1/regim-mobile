import styled from 'styled-components/native';
import { myTheme } from '../Theme/custom-theme';

export const Container = styled.View`
  background-color: ${myTheme['color-primary-200']};
  align-items: center;
  justify-content: center;
  flex: 1;
`;
